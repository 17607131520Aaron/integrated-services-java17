<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.integrated.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.enterprise.integrated.entity.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="nickname" column="nickname" />
        <result property="email" column="email" />
        <result property="phone" column="phone" />
        <result property="avatar" column="avatar" />
        <result property="gender" column="gender" />
        <result property="status" column="status" />
        <result property="remark" column="remark" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="createBy" column="create_by" />
        <result property="updateBy" column="update_by" />
        <result property="deleted" column="deleted" />
        <result property="version" column="version" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, nickname, email, phone, avatar, gender, status, remark,
        create_time, update_time, create_by, update_by, deleted, version
    </sql>

    <select id="findByUsername" resultMap="UserResultMap" parameterType="string">
        SELECT <include refid="Base_Column_List" />
        FROM sys_user
        WHERE username = #{username}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="findByEmail" resultMap="UserResultMap" parameterType="string">
        SELECT <include refid="Base_Column_List" />
        FROM sys_user
        WHERE email = #{email}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="findByPhone" resultMap="UserResultMap" parameterType="string">
        SELECT <include refid="Base_Column_List" />
        FROM sys_user
        WHERE phone = #{phone}
          AND deleted = 0
        LIMIT 1
    </select>

</mapper>


