<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.integrated.mapper.RoleMapper">

    <resultMap id="RoleResultMap" type="com.enterprise.integrated.entity.Role">
        <id property="id" column="id" />
        <result property="roleName" column="role_name" />
        <result property="roleCode" column="role_code" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="createBy" column="create_by" />
        <result property="updateBy" column="update_by" />
        <result property="deleted" column="deleted" />
        <result property="version" column="version" />
    </resultMap>

    <sql id="Base_Column_List">
        r.id, r.role_name, r.role_code, r.status, r.create_time, r.update_time,
        r.create_by, r.update_by, r.deleted, r.version
    </sql>

    <select id="findRolesByUserId" resultMap="RoleResultMap" parameterType="long">
        SELECT <include refid="Base_Column_List" />
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND r.status = 1
    </select>

    <select id="findUserIdsByRoleId" resultType="long" parameterType="long">
        SELECT ur.user_id
        FROM sys_user_role ur
        WHERE ur.role_id = #{roleId}
    </select>

    <select id="pageRolesByUserId" resultMap="RoleResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND r.status = 1
    </select>

</mapper>


